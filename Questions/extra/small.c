#include <stdio.h>
#include <conio.h>
#include <math.h>

int main()
{

    float res = 27;
    int len = 3;
    int n = 5;
    res = res + pow(n, len);
    printf("%.f", res);
    return 0;
}